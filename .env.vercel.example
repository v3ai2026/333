# MagicAI v9.9 Vercel 环境变量模板
# 使用说明：
# 1. 复制此文件为 .env.vercel
# 2. 填入实际的生产环境值
# 3. 在 Vercel 控制面板中逐个添加这些变量
# 注意：NEVER commit 真实密码到 Git！

# ============================================
# Laravel 核心配置
# ============================================
APP_ENV=production
APP_DEBUG=false
APP_URL=https://mdio.shop
APP_NAME=MagicAI

# 使用 `php artisan key:generate --show` 生成
# 格式必须是 base64:... 开头
APP_KEY=base64:YOUR_APP_KEY_HERE

# ============================================
# 数据库配置（MySQL/MariaDB）
# ============================================
DB_CONNECTION=mysql
DB_HOST=your-mysql-host.com
DB_PORT=3306
DB_DATABASE=magicai
DB_USERNAME=magicai_user
DB_PASSWORD=your-secure-db-password-here
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# ============================================
# Redis 配置（缓存、会话、队列）
# ============================================
REDIS_HOST=your-redis-host.com
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password-optional
CACHE_DRIVER=redis
SESSION_DRIVER=redis
QUEUE_DRIVER=redis

# ============================================
# 身份验证和安全
# ============================================
SESSION_DOMAIN=.mdio.shop
SESSION_SECURE_COOKIES=true
SANCTUM_STATEFUL_DOMAINS=mdio.shop
SANCTUM_EXPIRATION=10080

# CORS 允许的域名（逗号分隔）
CORS_ALLOWED_ORIGINS=https://mdio.shop,https://www.mdio.shop

# ============================================
# 邮件配置
# ============================================
MAIL_DRIVER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password-here
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@mdio.shop
MAIL_FROM_NAME=MagicAI

# 备选：使用 SendGrid
# MAIL_DRIVER=sendgrid
# SENDGRID_API_KEY=your-sendgrid-key

# ============================================
# AWS S3 配置（文件上传存储）
# ============================================
FILESYSTEM_DISK=s3
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=magicai-uploads
AWS_URL=https://your-bucket.s3.amazonaws.com

# ============================================
# Stripe 支付（如果使用）
# ============================================
STRIPE_PUBLIC_KEY=pk_live_your-stripe-public-key
STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key

# ============================================
# 第三方 API 服务
# ============================================

# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_ORGANIZATION=your-organization-id

# Anthropic Claude
ANTHROPIC_API_KEY=your-anthropic-api-key

# Google Gemini
GOOGLE_AI_API_KEY=your-google-ai-api-key

# Azure OpenAI
AZURE_OPENAI_API_KEY=your-azure-openai-key
AZURE_OPENAI_ENDPOINT=https://your-instance.openai.azure.com/

# Elasticsearch（搜索引擎）
ELASTICSEARCH_HOST=your-elasticsearch-host.com:9200
ELASTICSEARCH_USERNAME=elastic
ELASTICSEARCH_PASSWORD=your-es-password

# Neo4j（知识图谱）
NEO4J_URI=bolt://your-neo4j-host.com:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-neo4j-password

# ============================================
# Webhook 和 API 密钥
# ============================================
WEBHOOK_SECRET=your-webhook-signing-key
API_RATE_LIMIT=100  # 每分钟请求数

# ============================================
# 日志和监控
# ============================================
LOG_CHANNEL=single
LOG_LEVEL=info
SENTRY_LARAVEL_DSN=your-sentry-dsn-url  # 可选错误追踪

# ============================================
# Vercel 特定配置
# ============================================
# Vercel 会自动提供以下变量（无需设置）
# VERCEL=1
# VERCEL_ENV=production
# VERCEL_URL=vercel-generated-url.vercel.app

# ============================================
# 可选功能开关
# ============================================
ENABLE_BRAIN_ENGINE=true
ENABLE_RAG_CHATBOT=true
ENABLE_VECTOR_SEARCH=true
ENABLE_SPIDER_SERVICE=true

# Celery（后台任务队列）- 如果 Vercel 不支持长时间任务
USE_VERCEL_BACKGROUND_JOBS=true
